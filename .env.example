# ProjectForge AI — Environment Variables Example
# Copy this file to .env and fill in your actual values

# ─── LLM Provider Selection ──────────────────────────────────────────────────
# Choose 'nvidia' or 'gemini' (defaults to 'nvidia' if NVIDIA_API_KEY is set, else 'gemini')
LLM_PROVIDER=nvidia

# ─── NVIDIA NIM / OpenAI-Compatible Configuration ────────────────────────────
NVIDIA_API_KEY=your_nvidia_api_key_here
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1
NVIDIA_MODEL=nvidia/nemotron-3-ultra-550b-a55b
# Optional model overrides:
# NVIDIA_ORCHESTRATOR_MODEL=meta/llama-3.1-70b-instruct
# NVIDIA_WORKER_MODEL=nvidia/nemotron-3-ultra-550b-a55b
# NVIDIA_REPORT_MODEL=nvidia/nemotron-3-ultra-550b-a55b

# ─── Latency & Token Limits ──────────────────────────────────────────────────
DEFAULT_MAX_TOKENS=1500
REPORT_MAX_TOKENS=4096
LLM_TIMEOUT=60.0

# ─── Google Gemini Configuration ─────────────────────────────────────────────
GOOGLE_API_KEY=your_gemini_api_key_here
ORCHESTRATOR_MODEL=gemini-2.5-flash
WORKER_MODEL=gemini-2.5-flash
REPORT_MODEL=gemini-2.5-flash

# ─── Optional: GitHub Personal Access Token ──────────────────────────────────
# Scopes needed: repo, project, issues
GITHUB_TOKEN=

# ─── Web Server Configuration ────────────────────────────────────────────────
WEB_HOST=127.0.0.1
WEB_PORT=8000
